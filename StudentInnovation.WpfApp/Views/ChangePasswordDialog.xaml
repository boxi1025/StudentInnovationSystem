<Window x:Class="StudentInnovation.WpfApp.Views.ChangePasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="修改密码" Height="350" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="修改密码" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,14"/>

        <TextBlock Grid.Row="1" Text="原密码" Margin="0,0,0,6"/>
        <PasswordBox x:Name="OldPasswordBox" Grid.Row="2" Margin="0,0,0,12" Height="30" />

        <TextBlock Grid.Row="3" Text="新密码" Margin="0,0,0,6"/>
        <PasswordBox x:Name="NewPasswordBox" Grid.Row="4" Margin="0,0,0,12" Height="30" />

        <StackPanel Grid.Row="5" Margin="0,0,0,0">
            <TextBlock Text="确认密码" Margin="0,0,0,6"/>
            <PasswordBox x:Name="ConfirmPasswordBox" Height="30"/>
        </StackPanel>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="确定" Width="90" Margin="0,0,10,0" IsDefault="True" Click="Ok_Click"/>
            <Button Content="取消" Width="90" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>

