<Window x:Class="StudentInnovation.WpfApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="登录 - 学生创新成果系统" Height="320" Width="460" WindowStartupLocation="CenterScreen"
        Background="{StaticResource PageBackgroundBrush}">
    <Window.InputBindings>
        <KeyBinding Key="Enter" Command="{Binding LoginCommand}" />
    </Window.InputBindings>
    <Grid Margin="18">
        <Border Style="{StaticResource CardBorderStyle}">
            <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="系统登录" FontSize="22" FontWeight="Bold" Margin="0,0,0,16"
                   Foreground="{StaticResource AccentTextBrush}"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="用户名" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="8,2,0,8"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="密码" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Margin="8,2,0,8"/>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2">
            <Button Content="登录" Width="96" HorizontalAlignment="Right" Command="{Binding LoginCommand}" Margin="0,0,0,8"
                    Style="{StaticResource PrimaryButtonStyle}"/>
            <TextBlock Text="{Binding Message}" Foreground="Tomato" TextWrapping="Wrap"/>
        </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
